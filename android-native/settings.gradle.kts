pluginManagement {
    repositories {
        google {
            content {
                includeGroupByRegex("com\\.android.*")
                includeGroupByRegex("com\\.google.*")
                includeGroupByRegex("androidx.*")
            }
        }
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

// The standalone native Android app. It deliberately lives outside the Capacitor `android/`
// project: this is the from-scratch Kotlin/Compose rewrite, separate from the webapp shell.
rootProject.name = "GameNight"
include(":app")
